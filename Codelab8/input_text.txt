Now that you have a working solution using both the minimum line greedy algorithm and the dynamic programming pretty printing solution, run both of them using 40 characters maximum line width. Include a screenshot and compare the pretty printing cost function value for the two layouts (greedy and dynamic) as well as compare the two qualitatively. 